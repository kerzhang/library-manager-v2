extends layout.pug

block content
  body
    h2 New Book
    form
    if errors
      h4.error.danger Oooops! Errors returned!
      ul.error
      each error in errors
        li.danger= error.message
    else
      h5= error.message
    br
    a.btn.btn-warning(href= path) Return